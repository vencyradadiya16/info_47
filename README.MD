Simple Interest Calculatar application.

Calculates simple interest using the formula: SI = (P*R*N)/100.

#using the html,css,js.

#user-friendly application

#There are three file included index,css,  and js.


#installing Jenkins using Docker.
































git add .
git commit -m "Initial Implementation: SI Calculator"
git remote add origin <YOUR_GITHUB_REPO_URL>
git branch -M main
git push -u origin main



docker run -d -p 8080:8080 -p 50000:50000 -v jenkins_home:/var/jenkins_home --name jenkins jenkins/jenkins:lts




git add style.css
git commit -m "Improvement: Updated theme and button color"
git push origin main
